# My Start Project Repository

This repository is created for learning Git, GitHub, and Object-Oriented Programming (OOP) terminal workflows.

## Log of Changes Made:

### Day 1: Repository Setup
* Created this remote repository named `start` on GitHub.
* Configured global Git credentials (username and email) inside Git Bash.
* Successfully connected the local terminal to the online GitHub account using browser authentication.
* Created and pushed the initial `start.md` documentation file.

### Day 2: Adding Code Files & OOP Structure
* Successfully navigated into the project directory using the `cd` command.
* Created a C++ source file named `start.cpp` and pushed it to the main branch.
* Added a new Object-Oriented Programming file named `oop.cpp`.
* Implemented a standard C++ `department` class structure containing:
  * Private attributes: `dep_id`, `dep_name`, `no_of_teachers`, and `courses`.
  * Public encapsulation handlers: Secure getter and setter functions for all private variables.
* Successfully updated and pushed all local file modifications to the remote GitHub repository.
this is my start repository assignment 
